node_modules
.env
.expo
.rork

# vision golden set: local photos + dated reports stay out of git
expo/data/vision-golden-set/photos/
expo/data/vision-golden-set/report-*.json

# graphify knowledge-graph output (generated)
graphify-out/

# store screenshots are generated from the app (bun run screenshots) — the
# generator is tracked, the ~14MB of PNGs it produces are not
docs/store-screenshots/*/
docs/store-screenshots/MANIFEST.txt
